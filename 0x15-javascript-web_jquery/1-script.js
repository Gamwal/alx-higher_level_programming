#!/usr/bin/node

// Wait for the DOM to be ready
$(document).ready(function () {
    // Use the jQuery API to select and update the color of the header
    $('header').css('color', '#FF0000');
  });
  